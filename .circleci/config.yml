version: 2
jobs:
  abas:
    docker:
      - image: cimg/python:3.8

    steps:
    - checkout
    - run:
        name: SetUp_Lev
        command: |
          chmod u+x *
          sudo -u root | python main.py






workflows:
  version: 2
  abas:
    jobs:
    - abas
